"""Test suite for skyloc package.

Run tests with: pytest tests/ -v

Test markers:
    -m "not slow"      - Skip slow integration tests
    -m "not network"   - Skip tests requiring network access
    -m "slow"          - Run only slow tests
"""
